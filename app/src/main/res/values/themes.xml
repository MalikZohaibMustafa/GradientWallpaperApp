<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GradientWallpaper" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/app_background</item>
        <!-- Customize your theme here. -->
        <item name="menuItemTextColor">#FFFFFF</item> <!-- Custom attribute for menu item text color -->
    </style>
    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:titleTextColor">#FFFFFF</item>
        <item name="android:background">#000000</item> <!-- Optional: to set the background color -->
        <item name="android:buttonTint">#FFFFFF</item> <!-- Optional: to set the button text color -->
    </style>
    <style name="ToolbarMenuTextStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">?attr/menuItemTextColor</item>
    </style>
</resources>
